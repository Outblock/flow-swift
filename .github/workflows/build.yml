name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos11

    steps:
      - uses: actions/checkout@v2
      
      - name: Build
        run: swift build -v
        
      - name: Run tests
        run: swift test -v

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: norio-nomura/action-swiftlint@3.2.1
